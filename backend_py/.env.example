# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# CORS
FRONTEND_URL=http://localhost:5173

# Optional future integration keys (do not commit real values)
# GEMINI_API_KEY=
# or multiple keys (comma/semicolon/space-separated), first valid will be used
# GEMINI_API_KEYS=key1,key2
# GEMINI_MODEL=gemini-2.5-flash-image-preview
# GEMINI_TIMEOUT_MS=30000
# GEMINI_MAX_RETRIES=3
# AZURE_OPENAI_ENDPOINT=
# AZURE_OPENAI_KEY=
# AZURE_OPENAI_DEPLOYMENT_ID=
# AZURE_COMPUTER_VISION_ENDPOINT=
# AZURE_COMPUTER_VISION_KEY=

# Migration helper: if set, the Python backend will proxy generation requests
# to the specified base URL (e.g., http://localhost:3000)
# GENERATE_PROXY_TARGET=

# Optional: Gemini prompt tuning
# GEMINI_FIXED_PROMPT=wear these clothes naturally , do not change the persons face/identity/skin tone strictly.if there is no person in the image, generate a AI model wearing the clothes naturally, preserve natural fit, proportions, lighting and shadows.
# GEMINI_TEMPERATURE=0.2

# Alternative prompt (longer guidance)
# GEMINI_FIXED_PROMPT=Wear these clothes naturally. If the image contains clothing with a person form or face integrated, extract the clothing design (pattern, texture, silhouette) ONLY, and apply it naturally onto a new, AI-generated model. Do not change the original model's face/identity/skin tone IF the original model IS the focus. If no person is in the original image, or if the clothing is the sole focus, generate an AI model wearing the clothes naturally. Preserve natural fit, proportions, lighting, and shadows.

# Optional: External recommender service (positions-based)
# RECOMMENDER_URL=http://localhost:8081
# RECOMMENDER_TIMEOUT=10

# Optional: Internal positions-based recommender (file embeddings)
# POS_REC_EMBEDDINGS_PATH=./data/embeddings.npy

# Optional: Database connection for external services
# DB_HOST=
# DB_USER=
# DB_PASSWORD=
# DB_NAME=
# DB_PORT=
# DB_SSLMODE=
